name: generate_reflection
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  analyzer: ^6.2.0
  build: ^2.0.0
  universal_html:
    path: '..'

dev_dependencies:
  build_runner: ^2.4.6
  build_web_compilers: ^4.0.5
  lints: ^2.0.1